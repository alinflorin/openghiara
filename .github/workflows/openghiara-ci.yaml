name: openghiara Docker CI

on:
  push:
    branches:
      - main
    paths: 
      - src/openghiara/**

  pull_request:
    branches:
      - main
    paths: 
      - src/openghiara/**

jobs:
  reusable-docker:
    permissions:
      actions: read
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-docker-ci.yaml
    with:
      project: 'openghiara'
      push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
      scan: true
      version: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && 'latest' || github.sha }}
    secrets: inherit