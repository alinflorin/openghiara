apiVersion: v1
kind: Service
metadata:
  name: {{ include "openghiara.fullname" . }}
spec:
  selector:
    app: {{ include "openghiara.fullname" . }}
  ports:
  - port: 6901
    targetPort: 6901
    name: vnc
  - port: 9090
    targetPort: 9090
    name: mcp