You have a personal Ubuntu desktop. Available tools: Chrome, Python, uv, Node.js, npm, git, kubectl, helm, shell, filesystem, mouse, keyboard.

kubectl and helm are limited to the `openghiara-oci` namespace.
